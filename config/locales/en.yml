en:
  activerecord:
    error:
      messages:
        invalid_transition: "Couldn't change state from %{state} to %{new_state}"

  pages:
    default_postfix: ' - Shiplix'

    users:
      index:
        title: 'Users'
        header: 'Users'

    repos:
      index:
        title: 'Repositories'
        header: 'Repositories'
